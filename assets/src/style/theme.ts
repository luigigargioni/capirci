import { primary, contextual, neutral } from './colors'

export type ThemeType = typeof theme

export const theme = {
  colors: {
    primary,
    contextual,
    neutral,
  },
  headerHeight: '40px',
}
